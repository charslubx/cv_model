# 认证服务 - 使用说明

## 🎯 一句话总结

**在接口中，你只需要：**

```python
user = request.state.user  # ✅ 所有认证逻辑自动完成！
```

---

## 📦 文件清单

| 文件 | 说明 | 用途 |
|------|------|------|
| **核心文件** |||
| `auth_service.py` | 认证服务类 | ✅ 必需 - 复制到项目 |
| `http_middleware.py` | HTTP 中间件 | ✅ 必需 - 复制到项目 |
| **文档** |||
| `QUICK_START.md` | **⭐ 快速开始** | **新手必看** |
| `simple_usage_example.py` | 完整使用示例 | 实际代码示例 |
| `迁移对比.md` | 新旧对比 | 了解改进点 |
| `AUTH_MIGRATION_GUIDE.md` | 详细迁移指南 | 迁移时参考 |
| `AUTH_SERVICE_README.md` | API 文档 | 查阅 API |
| `test_auth_service.py` | 单元测试 | 测试参考 |

---

## 🚀 30 秒快速开始

### 1️⃣ 添加中间件

```python
from fastapi import FastAPI
from http_middleware import HttpMiddleware

app = FastAPI()

# 配置
class Config:
    ldap_server_url = "ldap://your-server.com"
    ldap_base_dn = "DC=example,DC=com"
    ldap_timeout = 30

# 添加中间件
app.add_middleware(HttpMiddleware, config=Config())
```

### 2️⃣ 在接口中使用

```python
@app.get("/api/user")
async def get_user(request: Request):
    user = request.state.user  # ✅ 就这么简单！
    
    if not user:
        return {"error": "未认证"}
    
    return {
        "idsid": user.idsid,
        "name": user.name,
        "email": user.email,
    }
```

### 3️⃣ 完成！🎉

**就是这么简单！** 所有认证逻辑（LDAP 查询、用户创建、信息刷新等）都在 service 中自动完成。

---

## 📖 推荐阅读顺序

### 新手入门

1. **阅读** `QUICK_START.md` → 了解基本使用
2. **运行** `simple_usage_example.py` → 看实际代码
3. **开始使用** → 在你的项目中添加中间件

### 迁移现有项目

1. **阅读** `迁移对比.md` → 了解改进点
2. **阅读** `AUTH_MIGRATION_GUIDE.md` → 详细迁移步骤
3. **运行** `test_auth_service.py` → 测试验证

### 深入了解

1. **阅读** `AUTH_SERVICE_README.md` → API 文档
2. **查看** `auth_service.py` → 源代码
3. **扩展** → 继承服务类实现自定义逻辑

---

## 💡 核心特点

### ✅ 使用简单

```python
# 接口中只需一行代码
user = request.state.user
```

### ✅ 自动处理

- 自动从 IIS 获取 REMOTE_USER
- 自动查询或创建用户
- 自动刷新过期信息（>30天）
- 自动更新登录时间

### ✅ 职责分离

- 中间件：编排请求流程
- 服务类：处理业务逻辑
- 接口：只关注业务

### ✅ 易于测试

```python
# 可以独立测试认证服务
auth_service = AuthService(...)
user = await auth_service.authenticate_user(request, db)
assert user.idsid == "expected"
```

### ✅ 易于扩展

```python
# 继承服务类扩展功能
class CustomAuthService(AuthService):
    async def authenticate_user(self, request, db):
        user = await super().authenticate_user(request, db)
        # 添加自定义逻辑
        return user
```

---

## 🎯 实际使用示例

### 获取用户信息

```python
@app.get("/api/user/profile")
async def get_profile(request: Request):
    user = request.state.user
    if not user:
        return JSONResponse(status_code=401, content={"error": "未认证"})
    
    return {"profile": {"idsid": user.idsid, "name": user.name}}
```

### 创建数据

```python
@app.post("/api/article/create")
async def create_article(request: Request):
    user = request.state.user
    if not user:
        return JSONResponse(status_code=401, content={"error": "未认证"})
    
    body = await request.json()
    article = {
        "title": body["title"],
        "author": user.idsid,  # 使用认证用户
    }
    return {"article": article}
```

### 权限检查

```python
@app.get("/api/admin/settings")
async def admin_settings(request: Request):
    user = request.state.user
    if not user:
        return JSONResponse(status_code=401, content={"error": "未认证"})
    if not user.is_admin:
        return JSONResponse(status_code=403, content={"error": "需要管理员权限"})
    
    return {"settings": {...}}
```

### 可选认证

```python
@app.get("/api/articles")
async def list_articles(request: Request):
    user = request.state.user
    
    # 已认证用户看到更多内容
    if user:
        return {"articles": [...], "recommended": [...]}
    else:
        return {"articles": [...]}  # 游客只看基础内容
```

---

## 📋 request.state 可用属性

| 属性 | 类型 | 说明 |
|------|------|------|
| `request.state.user` | User 或 None | **当前认证用户** |
| `request.state.idsid` | str 或 None | 用户 ID |
| `request.state.request_id` | str | 请求唯一标识 |
| `request.state.start_time` | datetime | 请求开始时间 |
| `request.state.server_name` | str | 服务器主机名 |

---

## 🔧 配置方式

### 方式 1: 配置对象（推荐）

```python
class Config:
    ldap_server_url = "ldap://ldap.example.com"
    ldap_base_dn = "DC=example,DC=com"
    ldap_timeout = 30

app.add_middleware(HttpMiddleware, config=Config())
```

### 方式 2: 环境变量

```bash
export LDAP_SERVER_URL="ldap://ldap.example.com"
export LDAP_BASE_DN="DC=example,DC=com"
export LDAP_TIMEOUT="30"
```

```python
# 自动从环境变量读取
app.add_middleware(HttpMiddleware)
```

---

## 🧪 运行示例

```bash
# 运行完整示例
python simple_usage_example.py

# 访问 API 文档
http://localhost:8000/docs

# 测试接口
curl http://localhost:8000/api/user/info
```

---

## 🧪 运行测试

```bash
# 所有测试
pytest test_auth_service.py -v

# 带覆盖率
pytest test_auth_service.py --cov=auth_service

# 特定测试
pytest test_auth_service.py::TestAuthService::test_authenticate_user_success -v
```

---

## 🚨 必需配置

### 实现数据库会话获取

在 `http_middleware.py` 中找到 `_get_db_session()` 方法：

```python
async def _get_db_session(self):
    """获取数据库会话"""
    # 方案1: 使用全局对象
    from your_project.globals import g
    return g.user_db_async_session()
    
    # 方案2: 使用 SQLAlchemy
    # from your_project.database import async_session_maker
    # async with async_session_maker() as session:
    #     yield session
```

### 更新导入路径

在 `auth_service.py` 中：

```python
# 更新为你的实际路径
from your_project.auth import UserBackend
from your_project.services import UserService
```

---

## 🎓 学习路径

### 第 1 天：快速上手
- ✅ 阅读 `QUICK_START.md`
- ✅ 运行 `simple_usage_example.py`
- ✅ 在项目中添加中间件

### 第 2 天：深入理解
- ✅ 阅读 `迁移对比.md`
- ✅ 查看 `auth_service.py` 源码
- ✅ 运行 `test_auth_service.py`

### 第 3 天：高级使用
- ✅ 阅读 `AUTH_SERVICE_README.md`
- ✅ 实现自定义认证逻辑
- ✅ 编写自己的测试

---

## 📞 常见问题

### Q: 如何获取用户信息？

```python
user = request.state.user  # ✅ 就这么简单
```

### Q: 如何检查用户是否认证？

```python
if not user:
    return JSONResponse(status_code=401, content={"error": "未认证"})
```

### Q: 接口代码需要改动吗？

不需要！如果你之前也是使用 `request.state.user`，接口代码完全不需要修改。

### Q: 性能如何？

几乎没有额外开销（< 1ms），可以忽略不计。

### Q: 如何扩展认证逻辑？

继承 `AuthService` 并重写方法即可：

```python
class CustomAuthService(AuthService):
    async def authenticate_user(self, request, db):
        user = await super().authenticate_user(request, db)
        # 添加自定义逻辑
        return user
```

---

## ✅ 快速检查清单

部署前检查：

- [ ] 已复制 `auth_service.py` 到项目
- [ ] 已复制 `http_middleware.py` 到项目
- [ ] 已配置中间件 `app.add_middleware(HttpMiddleware, config=...)`
- [ ] 已实现 `_get_db_session()` 方法
- [ ] 已更新导入路径（UserBackend, UserService）
- [ ] 已在测试环境验证
- [ ] 已运行单元测试

---

## 🎉 开始使用

```bash
# 1. 复制核心文件
cp auth_service.py your_project/
cp http_middleware.py your_project/

# 2. 添加中间件
# 在你的 app.py 中：
# app.add_middleware(HttpMiddleware, config=Config())

# 3. 在接口中使用
# user = request.state.user

# 4. 完成！🎉
```

---

## 📚 文档索引

| 文档 | 内容 | 适合 |
|------|------|------|
| `QUICK_START.md` | 快速开始 | 新手入门 |
| `simple_usage_example.py` | 实际代码 | 代码参考 |
| `迁移对比.md` | 新旧对比 | 了解改进 |
| `AUTH_MIGRATION_GUIDE.md` | 迁移指南 | 项目迁移 |
| `AUTH_SERVICE_README.md` | API 文档 | 深入学习 |
| `test_auth_service.py` | 单元测试 | 测试参考 |

---

## 💎 核心理念

> **在接口中，你只需要关心业务逻辑。**  
> **所有认证细节都在 service 中自动处理。**

这就是我们的设计目标！🎯

---

**立即开始：**

1. 📖 阅读 `QUICK_START.md`
2. ▶️ 运行 `simple_usage_example.py`
3. 🚀 在你的项目中使用

祝你使用愉快！🎉
